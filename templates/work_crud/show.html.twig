{% extends 'base.html.twig' %}

{% block title %}Work{% endblock %}

{% block body %}
    <h1>Work</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ work.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ work.name }}</td>
            </tr>
            <tr>
                <th>GithubLink</th>
                <td>{{ work.githubLink }}</td>
            </tr>
            <tr>
                <th>WebsiteLink</th>
                <td>{{ work.websiteLink }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td><img style="width: 500px" src="data:image/png;base64,{{ work.image }}"/>

            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_work_crud_index') }}">back to list</a>

    <a href="{{ path('app_work_crud_edit', {'id': work.id}) }}">edit</a>

    {{ include('work_crud/_delete_form.html.twig') }}
{% endblock %}
